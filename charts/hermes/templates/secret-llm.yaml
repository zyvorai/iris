{{- if and .Values.server.llm.apiKey (not .Values.server.llm.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: hermes-llm
  labels:
    app.kubernetes.io/name: hermes
type: Opaque
stringData:
  apiKey: {{ .Values.server.llm.apiKey | quote }}
{{- end }}
